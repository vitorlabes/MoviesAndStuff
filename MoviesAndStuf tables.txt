CREATE TABLE Movies (
    Id BIGINT IDENTITY(1,1) PRIMARY KEY,
    Title VARCHAR(50) NOT NULL,
    Review VARCHAR(200) NULL,
    Director VARCHAR(50) NULL,
    GenreId VARCHAR(15) NULL,
    Duration INTEGER NULL,
	Rating DECIMAL (3,1) NULL,
    PremiereDate DATE NULL,
	WatchDate DATE NULL,
	IsWatched BIT NOT NULL DEFAULT 0

    CONSTRAINT FK_Movies_Genre FOREIGN KEY (GenreId) REFERENCES Genres(Id)
);


CREATE TABLE Genres (
    Id VARCHAR(15) NOT NULL PRIMARY KEY,
    Name VARCHAR(15) NOT NULL,
	[Order] INT NOT NULL,
    IsActive BIT NOT NULL DEFAULT 1
);

INSERT INTO Genres
(Id, Name, Active, [Order])
VALUES('ACTION', 'Action', 1, 1),
('ADVENTURE', 'Adventure', 1, 2)
('THRILLER', 'Thriller', 1, 3),
('ROMANCE', 'Romance', 1, 4),
('ROMCOM', 'Romantic Comedy', 1, 5),
('COMEDY', 'Comedy', 1, 6),
('HORCOM', 'Horror Comedy', 1, 7)